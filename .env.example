# Server-only Lovable AI Gateway credentials. Never expose these as VITE_* variables.
LOVABLE_API_KEY=

# Optional: override this only when the configured Lovable model becomes unavailable.
LOVABLE_MODEL=google/gemini-3-flash-preview
